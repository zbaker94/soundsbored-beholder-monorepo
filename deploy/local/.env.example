# Local stack overrides (all optional — the compose defaults work with no .env).
# Copy to deploy/local/.env only if you want to change these.

# ROOM_PASSWORD=test
# LIVEKIT_API_KEY=devkey
# LIVEKIT_API_SECRET=secret
