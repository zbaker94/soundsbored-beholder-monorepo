{
  "name": "@soundsbored/listener",
  "version": "0.0.1",
  "description": "Standalone self-host web listener for SoundsBored remote audio",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fontsource-variable/figtree": "^5.0.0",
    "@soundsbored/contract": "*",
    "@soundsbored/core": "*"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
