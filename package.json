{
  "name": "soundsbored-remote-audio",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b packages/contract packages/relay",
    "test": "vitest run --reporter=verbose",
    "lint": "tsc -b packages/contract packages/relay --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
